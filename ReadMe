This is read me for Word_Puzzle program(is a game actually)
This can be used to solve any word puzzle that takes two inputs(eg: Word Link): 
  1.count of letter(s) that the word will contain
  2.list of lettes the word will comprise of 

@HOW THE PROGRAM WORK
  Run the file named ''
  You will be asked for two inputs:
    1st: count of lettes 
    2nd: lettes(enter without any spaces in them)
  Then the program will search into its database and find all the appropriate word that matches to the constraints and present them before you. 
@HOW TO RUN THE PROG:
